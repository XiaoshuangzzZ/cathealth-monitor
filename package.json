{
  "name": "cathealth-app",
  "version": "1.0.0",
  "description": "CatHealth Monitor - Pet Health Tracking App",
  "main": "index.html",
  "scripts": {
    "build:www": "node build-www.js",
    "sync:android": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "npm run build:www && npm run sync:android && cd android && gradlew.bat assembleDebug",
    "build:apk:unix": "npm run build:www && npm run sync:android && cd android && ./gradlew assembleDebug"
  },
  "keywords": ["cat", "health", "pet", "monitor"],
  "author": "CatHealth Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/camera": "^5.0.0",
    "@capacitor/geolocation": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
